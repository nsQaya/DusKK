@using Abp.Runtime.Session
@using TDV.Web.Areas.Portal.Models.Layout
@using TDV.Web.Areas.Portal.Views.Shared.Components.PortalLogo
@model HeaderViewModel
@{
    Layout = null;
}
<a href="@Url.Action("Index", "Home", new {area = string.Empty})" target="_blank">
    @await Component.InvokeAsync(typeof(PortalLogoViewComponent), new
    {
        logoSkin = "dark",
        logoClass = "h-30px logo"
    })
</a>
<a href="@Url.Action("Index", "Home", new {area = string.Empty})" target="_blank" class="d-lg-none">
    @await Component.InvokeAsync(typeof(PortalLogoViewComponent), new
    {
        logoSkin = "dark-sm",
        logoClass = "h-30px logo"
    })
</a>
